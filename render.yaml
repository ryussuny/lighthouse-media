services:
  - type: web
    name: lighthouse-media
    runtime: node
    buildCommand: npm install
    startCommand: node dashboard/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
